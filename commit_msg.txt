Scaffold deployment module template and configure DNS mapping

- Scaffolded the preprod-deploy/ sub-project using create-mn-app with the hello-world template.
- Resolved local Docker networking issues inside preprod-deploy/docker-compose.yml by mapping public DNS servers (8.8.8.8 and 8.8.4.4) to the proof-server container.
- Cleaned up the nested .git repository inside the scaffolded workspace to track it as a standard workspace folder.
- Generated the deployment wallet address and initialized the ledger sync process on the PreProd test network.
